#ifndef KEYSHARP_INPUTD_GLOBALS_H
#define KEYSHARP_INPUTD_GLOBALS_H

#include <stdbool.h>

/* Enabled by --verbose. Gates per-event debug logging throughout the daemon. */
extern bool g_verbose;

#endif
