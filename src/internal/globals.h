#ifndef KEYSHARP_INPUT_INTERNAL_GLOBALS_H
#define KEYSHARP_INPUT_INTERNAL_GLOBALS_H

#include <stdbool.h>

/* Enabled by --verbose. Gates per-event debug logging throughout the daemon. */
extern bool g_verbose;

#endif
